---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { Container, Section } from '@/components/ui';
import { BlogCard } from '@/components/blog';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';
import { getCollection } from 'astro:content';

// Get all blog posts from content collections
const allPosts = await getCollection('blog');

// Filter out drafts and sort by date (most recent first)
const sortedPosts = allPosts
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .map((post) => ({
    ...post.data,
    slug: post.slug,
  }));
---

<BaseLayout
  title="Blog | Facundo Zupel - Organic Growth y Automatizaciones"
  description="Artículos sobre crecimiento orgánico, automatizaciones empresariales, SEO, análisis de datos y estrategias de marketing digital."
>
  <Header slot="header" client:load />
  <Footer slot="footer" client:load />
  {/* Hero del Blog */}
  <section class="py-16 md:py-24">
    <Container size="xl">
      <div class="text-center max-w-3xl mx-auto">
        <h1
          class="text-4xl md:text-5xl lg:text-6xl font-display font-bold text-white mb-4"
        >
          Blog
        </h1>
        <p class="text-lg md:text-xl text-neutral-400 leading-relaxed">
          Aprende estrategias, técnicas y casos prácticos sobre <strong
            class="text-white">crecimiento orgánico</strong
          >, <strong class="text-white">automatizaciones</strong> y <strong
            class="text-white">análisis de datos</strong
          > para escalar tu negocio.
        </p>
      </div>
    </Container>
  </section>

  {/* Grid de artículos */}
  <section class="py-16 md:py-24">
    <Container size="xl">
      {
        sortedPosts.length === 0 ? (
          <div class="text-center py-16">
            <p class="text-xl text-neutral-400">
              Próximamente publicaremos nuevos artículos.
            </p>
          </div>
        ) : (
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {sortedPosts.map((post) => (
              <BlogCard post={post} client:load />
            ))}
          </div>
        )
      }
    </Container>
  </section>

  {/* CTA final */}
  <section class="py-16 md:py-24">
    <Container size="lg">
      <div class="max-w-3xl mx-auto text-center py-12 glass rounded-3xl p-8 md:p-12">
        <h2 class="text-3xl md:text-4xl font-display font-bold text-white mb-4">
          ¿Quieres recibir más contenido como este?
        </h2>
        <p class="text-lg text-neutral-400 mb-8">
          Suscríbete para recibir artículos sobre crecimiento orgánico y automatizaciones
          directamente en tu correo.
        </p>
        <button
          data-open-contact
          class="inline-block px-8 py-4 bg-accent-500 text-white font-semibold rounded-xl hover:bg-accent-600 hover:scale-105 transition-all shadow-lg hover:shadow-orange cursor-pointer btn-shine"
        >
          Contactar
        </button>
      </div>
    </Container>
  </section>
</BaseLayout>
