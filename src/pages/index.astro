---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Header } from '../components/layout/Header';
import { Footer } from '../components/layout/Footer';
import { Container } from '../components/ui';
import TrustBar from '../components/sections/TrustBar';
import PainPoints from '../components/sections/PainPoints';
import CaseStudyPreview from '../components/sections/CaseStudyPreview';
import Services from '../components/sections/Services';
import WhyMe from '../components/sections/WhyMe';
import WorkflowProcess from '../components/sections/WorkflowProcess';
import { ProfitabilityCalculator } from '../components/sections/ProfitabilityCalculator';
import FeaturedArticles from '../components/sections/FeaturedArticles';
import { StickyContact } from '../components/ui/StickyContact';
---

<BaseLayout
  title="Consultor SEO Chile | Posicionamiento Web y Crecimiento Orgánico — Facundo Zupel"
  description="Consultor SEO en Chile con +5 años de experiencia. Posicionamiento web, SEO técnico y estrategia de crecimiento orgánico para empresas y pymes. Caso Endado: +560K€ en orgánico."
>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "Facundo Zupel - Consultor SEO",
      "url": "https://facundogrowth.com",
      "logo": "https://facundogrowth.com/logo.png",
      "sameAs": [
        "https://www.linkedin.com/in/facundo-zupel-b7513916a/"
      ],
      "contactPoint": {
        "@type": "ContactPoint",
        "contactType": "customer service",
        "availableLanguage": "Spanish"
      }
    })} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Facundo Zupel",
      "jobTitle": "Consultor SEO",
      "url": "https://facundogrowth.com",
      "sameAs": [
        "https://www.linkedin.com/in/facundo-zupel-b7513916a/"
      ],
      "knowsAbout": [
        "SEO", "Posicionamiento Web", "SEO Técnico", "Google Analytics 4",
        "Google Search Console", "Python", "Automatización SEO",
        "SEO Ecommerce", "SEO Local", "Keyword Research"
      ],
      "worksFor": {
        "@type": "Organization",
        "name": "Facundo Zupel - Consultor SEO"
      }
    })} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "Facundo Zupel - Consultor SEO Chile",
      "url": "https://facundogrowth.com",
      "description": "Consultoría SEO en Chile. Estrategia de posicionamiento web, SEO técnico, automatización y crecimiento orgánico para empresas y pymes.",
      "publisher": {
        "@type": "Person",
        "name": "Facundo Zupel"
      }
    })} />
  </Fragment>

  <Header slot="header" client:load />

  <!-- HERO -->
  <section id="inicio" class="min-h-screen flex items-center relative pt-16">
    <Container>
      <div class="max-w-4xl mx-auto text-center py-[120px] md:py-[160px]">
        <!-- Eyebrow -->
        <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded bg-white/[0.08] border border-white/[0.08] mb-8">
          <span class="w-2 h-2 rounded-full bg-green-500"></span>
          <span class="text-sm text-white/70">Para empresas que quieren crecer sin quemar presupuesto en ads</span>
        </div>

        <h1 class="text-white mb-8 max-w-3xl mx-auto">
          Consultor SEO, Organic Growth y Automatizaciones
        </h1>

        <p class="text-xl text-white/50 mb-10 leading-relaxed max-w-2xl mx-auto">
          Ayudo a empresas y pymes a lograr posicionamiento orgánico en Google mediante estrategia SEO, SEO técnico y automatización. Resultados medibles, no vanity metrics.
        </p>

        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <button
            data-open-contact
            class="inline-flex items-center justify-center gap-3 px-8 py-3.5 bg-[#0070F3] text-white font-medium rounded-lg hover:bg-[#005AC8] transition-all duration-200 cursor-pointer"
          >
            Agendar Diagnóstico Gratuito
            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </button>
          <a
            href="#casos"
            class="inline-flex items-center justify-center gap-2 px-8 py-3.5 border border-white/[0.2] text-white font-medium rounded-lg hover:bg-white/[0.05] hover:border-white/[0.3] transition-all duration-200"
          >
            Ver Resultados Reales
            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/>
            </svg>
          </a>
        </div>

      </div>
    </Container>
  </section>

  <!-- TRUST BAR -->
  <TrustBar client:load />

  <!-- PAIN POINTS -->
  <PainPoints client:load />

  <!-- CASO DE ÉXITO -->
  <section id="casos">
    <CaseStudyPreview client:load />
  </section>

  <!-- SERVICIOS -->
  <Services client:load />

  <!-- POR QUÉ YO -->
  <WhyMe client:load />

  <!-- PROCESO -->
  <WorkflowProcess client:load />

  <!-- ARTÍCULOS DESTACADOS -->
  <FeaturedArticles client:load />

  <!-- CALCULADORA -->
  <section id="calculadora" class="py-[120px] md:py-[160px]">
    <Container>
      <ProfitabilityCalculator client:load />
    </Container>
  </section>

  <!-- CTA FINAL -->
  <section id="contacto" class="py-[120px] md:py-[160px] border-t border-white/[0.08]">
    <Container>
      <div class="text-center max-w-3xl mx-auto">
        <h2 class="text-white mb-6">
          ¿Listo para ver tus propios resultados?
        </h2>
        <p class="text-xl text-white/50 mb-10 leading-relaxed">
          El primer paso es un diagnóstico gratuito donde analizamos tu situación actual
          y te muestro oportunidades concretas de crecimiento orgánico.
        </p>

        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-10">
          <button
            data-open-contact
            class="inline-flex items-center justify-center gap-3 px-10 py-4 bg-white text-black font-medium rounded-lg hover:bg-white/90 transition-all duration-200 cursor-pointer text-lg"
          >
            Agendar Diagnóstico Gratuito
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
            </svg>
          </button>
          <a
            href="/quien-soy"
            class="inline-flex items-center justify-center gap-2 px-10 py-4 border border-white/[0.2] text-white font-medium rounded-lg hover:bg-white/[0.05] hover:border-white/[0.3] transition-all duration-200 text-lg"
          >
            Conocer más sobre mí
          </a>
        </div>

        <!-- Trust indicators -->
        <div class="flex flex-wrap justify-center gap-8 text-white/40 text-sm">
          <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-white/50" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
            </svg>
            Sin compromiso
          </span>
          <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-white/50" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
            </svg>
            30 minutos
          </span>
          <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-white/50" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
            </svg>
            100% enfocado en tu negocio
          </span>
        </div>
      </div>
    </Container>
  </section>

  <Footer slot="footer" client:load />
  <StickyContact client:load />
</BaseLayout>
