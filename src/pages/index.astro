---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Header } from '../components/layout/Header';
import { Footer } from '../components/layout/Footer';
import { Container, Section, Heading, Button } from '../components/ui';
import TrustBar from '../components/sections/TrustBar';
import PainPoints from '../components/sections/PainPoints';
import CaseStudyPreview from '../components/sections/CaseStudyPreview';
import Services from '../components/sections/Services';
import WhyMe from '../components/sections/WhyMe';
import WorkflowProcess from '../components/sections/WorkflowProcess';
import { ProfitabilityCalculator } from '../components/sections/ProfitabilityCalculator';
import { StickyContact } from '../components/ui/StickyContact';
---

<BaseLayout
  title="Facundo Zupel | Consultor de Organic Growth y Automatizaciones"
  description="Escala tu negocio sin depender de publicidad paga. Consultoría especializada en SEO rentable, automatizaciones y análisis de datos."
>
  <Header slot="header" client:load />

  <!-- HERO: Hook + Promesa clara -->
  <Section id="inicio" background="gray">
    <Container>
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center min-h-[80vh] lg:min-h-[70vh]">
        <div class="space-y-6">
          <!-- Eyebrow con segmentación -->
          <p class="text-sm font-semibold uppercase tracking-wider text-primary-600">
            Para empresas que quieren crecer sin quemar presupuesto en ads
          </p>

          <Heading level={1} client:load>
            Convierto tu tráfico orgánico en ingresos
          </Heading>

          <p class="text-lg md:text-xl text-neutral-700 leading-relaxed">
            Soy <strong class="text-primary-800">Facundo Zupel</strong>, consultor especializado en
            <strong class="text-primary-800"> Organic Growth</strong>,
            <strong class="text-primary-800"> CRO</strong> y
            <strong class="text-primary-800"> Automatizaciones</strong>.
            Ayudo a empresas a construir sistemas de adquisición que generan demanda constante,
            sin depender de la inversión publicitaria.
          </p>

          <!-- CTAs escalonados -->
          <div class="flex flex-col sm:flex-row gap-4">
            <button
              data-open-contact
              class="inline-block px-8 py-3.5 bg-accent-500 text-white font-semibold rounded-lg hover:bg-accent-600 hover:scale-105 transition-all shadow-md hover:shadow-orange text-center cursor-pointer"
            >
              Agendar Diagnóstico Gratuito
            </button>
            <a
              href="#casos"
              class="inline-block px-8 py-3.5 bg-white border-2 border-primary-800 text-primary-800 font-semibold rounded-lg hover:bg-primary-50 transition-colors text-center"
            >
              Ver Resultados Reales →
            </a>
          </div>
        </div>

        <!-- Imagen -->
        <div class="hidden lg:block">
          <div class="relative aspect-square rounded-2xl overflow-hidden shadow-2xl">
            <img
              src="/assets/facu.png"
              alt="Facundo Zupel - Consultor de Organic Growth y Automatizaciones"
              class="w-full h-full object-cover"
              loading="eager"
            />
          </div>
        </div>
      </div>
    </Container>
  </Section>

  <!-- TRUST BAR: Prueba social rápida -->
  <TrustBar client:load />

  <!-- PAIN POINTS: Generar tensión emocional -->
  <PainPoints client:load />

  <!-- CASOS DE ÉXITO: Prueba de que funciona -->
  <section id="casos">
    <CaseStudyPreview client:load />
  </section>

  <!-- SERVICIOS: La solución (ahora con contexto) -->
  <Services client:load />

  <!-- POR QUÉ YO: Diferenciación -->
  <WhyMe client:load />

  <!-- PROCESO: Cómo trabajamos -->
  <WorkflowProcess client:load />

  <!-- CALCULADORA: Herramienta interactiva -->
  <Section id="calculadora" background="gray">
    <Container>
      <ProfitabilityCalculator client:load />
    </Container>
  </Section>

  <!-- CTA FINAL: Múltiples niveles de compromiso -->
  <Section id="contacto" background="primary">
    <Container>
      <div class="text-center max-w-3xl mx-auto">
        <Heading level={2} className="text-primary-900 mb-4" client:load>
          ¿Listo para dejar de depender de los ads?
        </Heading>
        <p class="text-primary-800 mb-8 text-lg">
          El primer paso es un diagnóstico gratuito donde analizamos tu situación actual
          y te muestro oportunidades concretas de crecimiento orgánico.
        </p>

        <!-- CTAs con diferentes niveles de compromiso -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <button
            data-open-contact
            class="inline-block px-8 py-4 bg-accent-500 text-white font-semibold rounded-lg hover:bg-accent-600 hover:scale-105 transition-all shadow-lg cursor-pointer"
          >
            Agendar Diagnóstico Gratuito
          </button>
          <a
            href="/quien-soy"
            class="inline-block px-8 py-4 bg-white/20 backdrop-blur-sm border border-primary-700 text-primary-900 font-semibold rounded-lg hover:bg-white/30 transition-colors text-center"
          >
            Conocer más sobre mí
          </a>
        </div>

        <!-- Elemento de reducción de fricción -->
        <p class="mt-6 text-sm text-primary-700">
          Sin compromiso. 30 minutos. 100% enfocado en tu negocio.
        </p>
      </div>
    </Container>
  </Section>

  <Footer slot="footer" client:load />
  <StickyContact client:load />
</BaseLayout>
