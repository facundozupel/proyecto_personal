# =============================================================================
# Environment Variables - Production
# =============================================================================
#
# IMPORTANTE:
# 1. Copiar este archivo como .env.production
# 2. Reemplazar los valores de ejemplo con los reales
# 3. NO commitear .env.production (está en .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# CMS Service (Backend API)
# -----------------------------------------------------------------------------

# Password para endpoints de administración del blog
# Usar un password fuerte y único
ADMIN_PASSWORD=tu_password_super_seguro_aqui

# Dominios permitidos para CORS (separados por coma)
# Incluir dominio de producción del frontend
ALLOWED_ORIGINS=https://facundogrowth.com,https://www.facundogrowth.com

# -----------------------------------------------------------------------------
# Frontend (Astro)
# -----------------------------------------------------------------------------

# URL de la API del CMS Service
# En producción apunta al subdominio api.facundogrowth.com
CMS_API_URL=https://api.facundogrowth.com

# -----------------------------------------------------------------------------
# Deployment Info
# -----------------------------------------------------------------------------

# Nombre del stack en Docker Swarm
STACK_NAME=facundogrowth

# Versión de las imágenes Docker (para tagging)
VERSION=1.0.0

# Registry de Docker Hub (si usas otro registry, cambiarlo)
DOCKER_REGISTRY=facundozupel

# -----------------------------------------------------------------------------
# Dominios
# -----------------------------------------------------------------------------

# Dominio principal del frontend
FRONTEND_DOMAIN=facundogrowth.com

# Subdominio de la API
API_DOMAIN=api.facundogrowth.com

# -----------------------------------------------------------------------------
# Notas
# -----------------------------------------------------------------------------
#
# 1. Asegúrate de que los DNS estén configurados:
#    - facundogrowth.com → IP de tu VPS
#    - api.facundogrowth.com → IP de tu VPS
#
# 2. Traefik debe estar corriendo en tu VPS con:
#    - Red "network_public" creada
#    - Certificados Let's Encrypt configurados
#
# 3. Para cargar estas variables antes del deploy:
#    export $(cat .env.production | grep -v '^#' | xargs)
#
# =============================================================================
